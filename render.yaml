services:
  - type: web
    name: api-watchdog
    env: static
    buildCommand: echo "No build required for static site"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html